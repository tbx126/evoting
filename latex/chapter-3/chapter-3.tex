\chapter{System Model and Methodology}
\begingroup
\justifying
\setlength{\parindent}{0pt}
\setstretch{1.5}
\setlength{\parskip}{0.5\baselineskip}
\titlespacing{\chapter}{0pt}{0pt}{0pt}
\titlespacing{\section}{0pt}{0pt}{0pt}

\section{Protocol Roles}

There are three roles in the protocol: administrator, voters and third-party auditor.

\textbf{Administrator.} The administrator is the entity holding the election. It deploys smart contracts, manages the whitelist of voters' address and retains the ElGamal private key $\mathit{sk}$. When the election comes to the end, it is responsible for performing homomorphic aggregation and submitting the tally results together with a zero-knowledge proof (ZKP3).

\textbf{Voter.} Any voter can use their address (on the whitelist) to cast a ballot by encrypting a one-hot vector and generating ZKP1 and ZKP2 proofs. Once the ballot is accepted, they can retain a local receipt for subsequent ballot inclusion verification. The encryption and generation are automatically calculated by scripts; all voters only need to do is choose their preferred candidate and sign the ballot with crypto wallet.

\textbf{Public auditor.} Any party able to read blockchain data with no credentials: they can see on-chain state transitions, verify proofs of verification results and perform the auditing protocol from Section~3.9 with the public auditable package.


\section{Threat Model}

The model considers malicious voters and passive blockchain observers. For administrator behaviour, the prototype assumes a \emph{semi-trusted administrator}: the administrator is expected to follow the protocol, while single-key control remains an explicit trust assumption rather than a cryptographically enforced guarantee. The following attacks are explicitly targeted:
\begin{itemize}
    \item invalid ballot shape (non-one-hot encryption);
    \item out-of-range candidate encoding;
    \item replay or duplicate voting attempts;
    \item fabricated tally claims unsupported by valid proof.
\end{itemize}

The following are not fully solved in the current prototype: coercion resistance, malware-compromised browser clients, threshold key governance, fully adversarial administrator behavior (for example refusing to tally or submitting inconsistent off-chain artifacts), and per-candidate integer-to-result-point binding at contract layer (ZKP3 constrains curve-point correctness, while integer mapping remains client-verifiable rather than contract-enforced). Table~\ref{tab:threat-matrix} structures the full attack surface considered by this model.

\begin{table}[htbp]
\centering
\small
\caption{Threat Model Mapping Matrix}
\label{tab:threat-matrix}
\begin{tabular}{L{2.0cm} L{2.6cm} L{2.2cm} L{2.8cm} L{2.8cm}}
\toprule
Attacker & Attack method & Target property & Control mechanism & Residual risk \\
\midrule
Malicious voter & Submit non-one-hot or out-of-range encoding & Tally integrity & ZKP1/2 + contract range checks & Compromised client runtime can still submit seemingly valid inputs \\
Malicious voter & Replay/duplicate voting & One-person-one-vote & Registry whitelist + hasVoted gate & Credential theft/abuse is outside this model \\
Semi-trusted administrator & Infer individual ballot content & Ballot secrecy & ElGamal encryption + aggregate-only decryption + ZKP3 & Single-key custody remains a trust assumption \\
Adversarial administrator & Refuse tally or submit inconsistent off-chain artifacts & Availability and auditability & On-chain state machine + proof gates & Denial-of-service and off-chain artifact inconsistency not fully eliminated \\
Adversarial administrator & Alter per-candidate integer distribution (same total) & Per-candidate tally authenticity & Client-side point-to-integer recheck & Contract layer does not strongly bind integers to result points (see Section~5.8, \textbf{[P1]}) \\
\bottomrule
\end{tabular}
\end{table}

\section{Protocol State Machine}

Election lifecycle states are:
\begin{equation}
S = \{\texttt{Created},\ \texttt{Active},\ \texttt{Ended},\ \texttt{Tallied}\}
\end{equation}

Allowed transitions are:
\begin{equation}
\texttt{Created} \rightarrow \texttt{Active} \rightarrow \texttt{Ended} \rightarrow \texttt{Tallied}
\end{equation}

This deterministic state machine ensures that vote submission only occurs in the active phase and tally finalization only occurs after the election is ended.

Table~\ref{tab:state-transition-guards} makes the transition guards explicit at protocol level.

\begin{table}[htbp]
\centering
\small
\caption{State Transition Guards and Rejection Rules}
\label{tab:state-transition-guards}
\begin{tabular}{L{3.5cm} L{4.5cm} L{5.5cm}}
\toprule
Transition & Function & Guard condition \\
\midrule
\texttt{Created} $\rightarrow$ \texttt{Active} & \texttt{startElection()} & Candidate count equals circuit cardinality. \\
\texttt{Active} $\rightarrow$ \texttt{Ended} & \texttt{endElection()} & Current state is \texttt{Active}. \\
\texttt{Ended} $\rightarrow$ \texttt{Tallied} & \texttt{updateTallyResults()} & Valid ZKP3, matching public key signals, and total-vote consistency. \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:protocol-flow} illustrates the complete interaction procedure.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[x=1cm,y=1cm]
    \node[draw, rounded corners, minimum width=8.6cm, minimum height=0.95cm] (a) at (0,0) {1) Setup: admin deploys contracts and publishes ElGamal public key};
    \node[draw, rounded corners, minimum width=8.6cm, minimum height=0.95cm] (b) at (0,-1.5) {2) Vote casting: voter encrypts one-hot ballot and generates vote proof};
    \node[draw, rounded corners, minimum width=8.6cm, minimum height=0.95cm] (c) at (0,-3.0) {3) On-chain verification: contract checks Groth16 proof and records commitment};
    \node[draw, rounded corners, minimum width=8.6cm, minimum height=0.95cm] (d) at (0,-4.5) {4) Tally: admin aggregates ciphertexts, decrypts totals, submits ZKP3};
    \node[draw, rounded corners, minimum width=8.6cm, minimum height=0.95cm] (e) at (0,-6.0) {5) Audit: voter verifies receipt commitment against Merkle proof};

    \draw[->, thick] (a) -- (b);
    \draw[->, thick] (b) -- (c);
    \draw[->, thick] (c) -- (d);
    \draw[->, thick] (d) -- (e);
\end{tikzpicture}
\caption{End-to-End Protocol Flow}
\label{fig:protocol-flow}
\end{figure}

Figure~\ref{fig:protocol-sequence} provides a sequence-style view of the same workflow with explicit message directions.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[x=1cm,y=1cm]
    \node[draw, minimum width=2.8cm, minimum height=0.75cm] (voter) at (0,0) {Voter Client};
    \node[draw, minimum width=3.2cm, minimum height=0.75cm] (chain) at (5.0,0) {Voting Contract};
    \node[draw, minimum width=2.8cm, minimum height=0.75cm] (admin) at (10.0,0) {Admin Client};

    \draw[dashed] (0,-0.45) -- (0,-7.1);
    \draw[dashed] (5.0,-0.45) -- (5.0,-7.1);
    \draw[dashed] (10.0,-0.45) -- (10.0,-7.1);

    \draw[->, thick] (0,-1.0) -- (5.0,-1.0);
    \node[font=\scriptsize, align=center] at (2.5,-0.72) {castVote(commitment, ciphertextHash, ZKP1/2)};

    \draw[->, thick] (5.0,-1.8) -- (0,-1.8);
    \node[font=\scriptsize, align=center] at (2.5,-1.52) {transaction receipt (event confirms commitment on-chain)};

    \draw[->, thick] (10.0,-2.8) -- (5.0,-2.8);
    \node[font=\scriptsize, align=center] at (7.5,-2.52) {query encrypted vote events};

    \draw[->, thick] (5.0,-3.6) -- (10.0,-3.6);
    \node[font=\scriptsize, align=center] at (7.5,-3.32) {event logs for homomorphic aggregation};

    \draw[->, thick] (10.0,-4.8) -- (5.0,-4.8);
    \node[font=\scriptsize, align=center] at (7.5,-4.52) {updateTallyResults(results, root, ZKP3)};

    \draw[->, thick] (5.0,-5.8) -- (0,-5.8);
    \node[font=\scriptsize, align=center] at (2.5,-5.52) {public tally + merkleRoot for audit verification};
\end{tikzpicture}
\caption{Protocol Sequence Diagram for Voting and Tally}
\label{fig:protocol-sequence}
\end{figure}

\section{Ballot Encoding and Encryption}

Let $N$ be the number of candidates and $\mathit{candidateId} \in \{0,1,\dots,N-1\}$ be the selected candidate index. A ballot is encoded as one-hot vector
$\mathbf{m}=(m_0,\dots,m_{N-1})$ where $m_{\mathit{candidateId}}=1$ and all other entries are $0$.

For each candidate position $j$, ElGamal ciphertext is generated on BabyJubJub:
\begin{equation}
C1_j = r_j \cdot G, \quad C2_j = m_j \cdot G + r_j \cdot \mathit{PK}
\end{equation}
where $r_j$ is sampled uniformly from the BabyJubJub scalar field, $G$ is generator, and $\mathit{PK}$ is administrator public key. Here, $C1_j=(\mathit{c1X}_j,\mathit{c1Y}_j)$ and $C2_j=(\mathit{c2X}_j,\mathit{c2Y}_j)$ denote affine coordinates, which are used by the vote-proof witness and ciphertext binding hash. The notation is general in $N$, while the current deployed prototype instantiates $N=2$ and enforces this cardinality at contract layer.

\section{Commitment and Ciphertext Binding}

For private random salt $\mathit{salt}$, commitment is defined as:
\begin{equation}
\mathit{commitment} = \mathsf{Poseidon}(\mathit{candidateId},\, \mathit{salt})
\end{equation}

Ciphertext binding hash is defined over all ciphertext coordinates:
\begin{equation}
\begin{split}
H_{ct} = \mathsf{Poseidon}(&\mathit{c1X}_0,\,\mathit{c1Y}_0,\,\mathit{c2X}_0,\,\mathit{c2Y}_0,\,\dots,\\
                  &\mathit{c1X}_{N-1},\,\mathit{c1Y}_{N-1},\,\mathit{c2X}_{N-1},\,\mathit{c2Y}_{N-1})
\end{split}
\end{equation}

The vote proof uses $(\mathit{commitment}, H_{ct}, \mathit{PK}_x, \mathit{PK}_y)$ as public signals and proves consistency between commitment, candidate index, and encrypted one-hot structure.
Here, $\mathit{PK}_x, \mathit{PK}_y$ denote affine coordinates of $\mathit{PK}$.
In the reference tally client, each emitted ciphertext payload is re-hashed off-chain and matched against the on-chain vote record $(\mathit{commitment}, \mathit{ciphertextHash})$ before aggregation; mismatched entries are excluded from the tally set. This is a client-side integrity requirement rather than a contract-enforced invariant.

\section{Vote Proof Constraints}

The vote circuit takes private inputs $(\mathit{candidateId},\, \mathit{salt},\, r_j,\, \mathit{c1X}_j,\, \mathit{c1Y}_j,\, \mathit{c2X}_j,\, \mathit{c2Y}_j)$ for $j \in \{0,\dots,N{-}1\}$, and exposes four public signals $(\mathit{commitment},\, H_{ct},\, \mathit{PK}_x,\, \mathit{PK}_y)$. It enforces the following constraint families.
\begin{enumerate}
    \item \textbf{Commitment consistency.} The circuit recomputes $\mathsf{Poseidon}(\mathit{candidateId},\,\mathit{salt})$ from private inputs and constrains the result to equal the public signal $\mathit{commitment}$. This binds the on-chain commitment to a specific candidate choice without disclosing it.
    \item \textbf{Candidate range.} The circuit asserts $0 \le \mathit{candidateId} < N$, where $N$ is the candidate cardinality hardcoded at compilation time. Any index outside this range causes the constraint system to be unsatisfiable.
    \item \textbf{One-hot encryption.} For each slot $j \in \{0,\dots,N{-}1\}$, the circuit verifies that the ElGamal ciphertext encodes $m_j = 1$ when $j = \mathit{candidateId}$ and $m_j = 0$ otherwise. Together with the range constraint, this guarantees that every accepted ballot assigns exactly one vote to exactly one candidate.
    \item \textbf{Ciphertext binding.} The circuit hashes all $4N$ ciphertext coordinates $(\mathit{c1X}_0,\,\mathit{c1Y}_0,\,\mathit{c2X}_0,\,\mathit{c2Y}_0,\,\dots,\,\mathit{c1X}_{N-1},\,\mathit{c1Y}_{N-1},\,\mathit{c2X}_{N-1},\,\mathit{c2Y}_{N-1})$ with Poseidon and constrains the digest to equal the public signal $H_{ct}$. Because $H_{ct}$ is recorded on-chain alongside the commitment, any post-proof substitution of ciphertext components is detectable.
\end{enumerate}

\section{Homomorphic Tally and Tally Proof}

ElGamal encryption is additively homomorphic over point encodings, so aggregate vote totals are computed without decrypting individual ballots. For each candidate position $j$, the aggregated ciphertext is obtained by elliptic-curve point addition over all $V$ cast ballots:
\begin{equation}
\bar{C1}_j = \sum_{k=1}^{V} C1_{k,j}, \quad \bar{C2}_j = \sum_{k=1}^{V} C2_{k,j}
\end{equation}

Since the one-hot encoding maps each ballot to either $0 \cdot G$ or $1 \cdot G$, the aggregate decrypts as:
\begin{equation}
\bar{C2}_j - \mathit{sk} \cdot \bar{C1}_j = \left(\sum_{k=1}^{V} m_{k,j}\right) \cdot G = n_j \cdot G
\end{equation}
where $n_j$ is the integer vote count for candidate $j$. The result point for each candidate is therefore:
\begin{equation}
R_j = \bar{C2}_j - \mathit{sk} \cdot \bar{C1}_j
\end{equation}

Since $n_j \le V$, the integer $n_j$ is recovered from $R_j = n_j \cdot G$ by a bounded discrete-logarithm search. The administrator submits the recovered integer results together with a Groth16 tally proof.

The tally circuit takes $\mathit{sk}$ as its sole private input and exposes $(\mathit{PK}_x,\, \mathit{PK}_y,\, \bar{C1}^{X}_j,\, \bar{C1}^{Y}_j,\, \bar{C2}^{X}_j,\, \bar{C2}^{Y}_j,\, R^{X}_j,\, R^{Y}_j,\, \mathit{totalVotes})$ for $j \in \{0,\dots,N{-}1\}$ as public signals. It enforces three constraint families.
\begin{enumerate}
    \item \textbf{Key ownership.} The circuit verifies $\mathit{PK} = \mathit{sk} \cdot G$, confirming that the prover holds the secret key corresponding to the on-chain election public key.
    \item \textbf{Decryption correctness.} For each candidate $j$, the circuit constrains $R_j = \bar{C2}_j - \mathit{sk} \cdot \bar{C1}_j$, ensuring that each result point is the honest decryption of the corresponding aggregated ciphertext.
    \item \textbf{Sum consistency.} The circuit enforces $\sum_{j=0}^{N-1} R_j = \mathit{totalVotes} \cdot G$ in curve-point arithmetic, confirming that the result points collectively account for the declared total vote count.
\end{enumerate}

Before invoking the Groth16 verifier, the contract performs two pre-checks: the public-key coordinates in the proof's public signals must match the on-chain election key, and the $\mathit{totalVotes}$ signal must equal the contract's internal vote counter. If the proof is accepted, the contract stores the integer results and the Merkle root, then transitions to the \texttt{Tallied} state.

\section{Merkle Audit Construction}

A Merkle tree over all $V$ ballot commitments provides $O(\log V)$ inclusion proofs, enabling any voter or auditor to verify that a specific ballot commitment is part of the finalised set. Each leaf holds one ballot commitment:
\begin{equation}
\mathit{leaf}_i = \mathit{commitment}_i
\end{equation}

Adjacent nodes are combined upward through Keccak256 to form each internal node, with the resulting root stored on-chain at tally finalisation:
\begin{equation}
\mathit{node} = \mathsf{Keccak256}(\mathit{left} \parallel \mathit{right})
\end{equation}

Internal nodes use Keccak256 to make the tree compatible with EVM-native audit tooling, and leaves are the Poseidon commitments already generated by the vote circuit. If a layer is of an odd cardinality, the last node is copied. Commitments are ordered according to \texttt{blockNumber}, \texttt{transactionIndex}, \texttt{logIndex} prior to tree construction; this gives rise to deterministic roots if and only if all realisations use the same ordering. Once the tally is finalised, Merkle root updates are rejected by a \texttt{Tallied}-state guard and the audit digest remains immutable on-chain.

\section{Anonymous Verification Procedure}

The anonymous verification procedure allows any voter to confirm that their ballot commitment is included in the finalised audit set without disclosing their identity or ballot content. The voter holds a local receipt with five fields: \textit{candidateId}, \textit{salt}, \textit{commitment}, \textit{txHash}, and \textit{electionId}.

Verification proceeds through four sequential steps:
\begin{enumerate}
    \item Recompute $\mathsf{Poseidon}(\mathit{candidateId},\, \mathit{salt})$ from the receipt and confirm it matches the stored commitment.
    \item Locate the audit bundle entry whose commitment and transaction hash both match the receipt.
    \item Verify the Merkle inclusion path from the matched leaf to the bundle root using the provided sibling hashes.
    \item Accept if and only if the path is valid, the bundle root equals the on-chain \texttt{merkleRoot}, and the election identifier matches.
\end{enumerate}

Because the procedure relies on commitment and transaction hash rather than the voter's address, no address-based lookup is required. However, on-chain vote events still record the sender address alongside the commitment; this linkage is visible at the chain layer and falls outside the scope of this verification-flow abstraction.

Table~\ref{tab:audit-artifacts} summarises the artifacts involved in anonymous verification.

\begin{table}[htbp]
\centering
\small
\caption{Audit Artifacts and Their Roles}
\label{tab:audit-artifacts}
\begin{tabular}{L{3.2cm} L{3.8cm} L{5.8cm}}
\toprule
Artifact & Source & Verification Role \\
\midrule
Receipt JSON & Voter local storage & Provides candidate ID, salt, and commitment to recompute commitment consistency. \\
Commitment leaf & On-chain vote event & Represents one ballot commitment in Merkle construction. \\
Proof path & Audit bundle entry & Enables logarithmic-size inclusion verification against bundle root. \\
Merkle root & Tally publication / bundle metadata & Serves as commitment set digest to validate inclusion outcome. \\
Election ID & Bundle + receipt metadata & Prevents cross-election replay or accidental bundle mismatch. \\
\bottomrule
\end{tabular}
\end{table}

\endgroup

