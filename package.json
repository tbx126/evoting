{
  "name": "blockchain-evoting",
  "version": "1.0.0",
  "description": "基于区块链的电子投票系统 - Blockchain-Based E-Voting System",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy-local.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node",
    "demo": "hardhat run scripts/deploy-local.js --network localhost",
    "server": "python -m uvicorn server:app --port 8000"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "dotenv": "^16.3.1",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.3"
  }
}
